{
    "name": "infinite-minesweeper",
    "version": "1.0.0",
    "description": "Minesweeper on an infinte board",
    "type": "module",
    "directories": {
        "test": "test"
    },
    "scripts": {
        "test": "node --experimental-vm-modules node_modules/jest/bin/jest.js --verbose --coverage --watchAll",
        "format": "prettier --write '**/*.{js,json,md}'",
        "lint": "eslint 'build/**/*.{js,json,md}' --fix",
        "gh-pages": "gh-pages -d build"
    },
    "repository": {
        "type": "git",
        "url": "git+https://github.com/ziap/infinite-minesweeper.git"
    },
    "author": "",
    "license": "ISC",
    "bugs": {
        "url": "https://github.com/ziap/infinite-minesweeper/issues"
    },
    "homepage": "infinite-minesweeper.vercel.app",
    "devDependencies": {
        "@types/jest": "^27.4.0",
        "eslint": "^8.8.0",
        "gh-pages": "^3.2.3",
        "jest": "^27.5.1",
        "jest-canvas-mock": "^2.3.1",
        "prettier": "^2.5.1"
    },
    "jest": {
        "testEnvironment": "jsdom",
        "setupFiles": [
            "jest-canvas-mock"
        ]
    }
}
