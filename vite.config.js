/**
 * @type {import('vite').UserConfig}
 */
const config = {
    base: '',
    root: 'src',
    build: {
        outDir: '../dist',
        polyfillModulePreload: false,
    }
}

export default config
